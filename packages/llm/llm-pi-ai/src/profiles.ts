/**
 * Explicit profile-resolution surface for managed adapter implementations.
 * Kept off the plugin package root so ordinary consumers see only the Cordis
 * plugin contract.
 * @module @deepseek-ai/dsh-llm-pi-ai/profiles
 */

export { resolveProfiles } from './config.ts'
export type { ResolvedPiAiProviderProfile } from './config.ts'
