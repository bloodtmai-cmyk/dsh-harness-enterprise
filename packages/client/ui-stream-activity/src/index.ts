/**
 * Stream Activity presentation plugin, node half.
 *
 * The browser half owns the visual treatment; the empty Host apply keeps the
 * package composable through the ordinary Cordis client-plugin roster.
 */

/** Host plugin body: this presentation plug-in has no Host behavior. */
export function apply(): void {}
