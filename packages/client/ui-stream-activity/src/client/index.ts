/** Compact conversation activity styling for the managed desktop surface. */
import './StreamActivity.module.css'

/** This CSS-only Client plug-in requires no runtime services. */
export const inject: readonly string[] = []

/** Client plug-in body: importing the stylesheet installs the presentation. */
export function apply(): void {}
