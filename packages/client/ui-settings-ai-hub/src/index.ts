/** Host loader entry for the AI Hub market browser implementation exported from `./client`. */

/** Host plugin body - no host-side behavior for the market contribution. */
export function apply(): void {}
